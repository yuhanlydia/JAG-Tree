base_config: ../goav.yaml
runtime:
  profile: smoke
execution:
  container_digest_required: false
seeds: [101, 202]
output:
  identity: goav_phase0_smoke
phase0:
  tasks_min: 4
  tasks_max: 8
  tests_per_task_min: 4
  tests_per_task_max: 6
  subset_draws_per_group_design: 8
